{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Iniciar Programa",
            "program": "${workspaceFolder}/src/server.js" // Ajuste o caminho conforme necessário
        }
    ]
}
